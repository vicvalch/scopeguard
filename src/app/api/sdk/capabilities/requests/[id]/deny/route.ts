import { createSupabaseServerClient } from "@/lib/supabase/server"; import { requireWorkspaceRole } from "@/lib/security/access-guards"; import { requireAuthenticatedUser } from "@/lib/security/server-authorization";
export async function POST(_: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const supabase = await createSupabaseServerClient(); const { user } = await requireAuthenticatedUser(); const { data: request } = await supabase.from("capability_requests").select("*").eq("id", id).maybeSingle(); if (!request) return Response.json({ error: "request_not_found" }, { status: 404 }); await requireWorkspaceRole(request.workspace_id, ["owner", "admin"]); await supabase.from("capability_requests").update({ status: "denied", evaluator_user_id: user.id, decided_at: new Date().toISOString(), updated_at: new Date().toISOString() }).eq("id", id); return Response.json({ ok: true }); }
