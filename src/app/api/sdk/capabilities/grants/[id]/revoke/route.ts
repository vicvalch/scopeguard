import { createSupabaseServerClient } from "@/lib/supabase/server"; import { requireWorkspaceRole } from "@/lib/security/access-guards"; import { requireAuthenticatedUser } from "@/lib/security/server-authorization";
export async function POST(_: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const supabase = await createSupabaseServerClient(); const { user } = await requireAuthenticatedUser(); const { data: grant } = await supabase.from("capability_grants").select("*").eq("id", id).maybeSingle(); if (!grant) return Response.json({ error: "grant_not_found" }, { status: 404 }); await requireWorkspaceRole(grant.workspace_id, ["owner", "admin"]); await supabase.from("capability_grants").update({ status: "revoked", revoked_at: new Date().toISOString(), revoked_by_user_id: user.id }).eq("id", id); return Response.json({ ok: true }); }
