import type { ExecutionSurvivabilityForecast, CriticalMilestone, ExecutionFragility, TemporalPressureCluster } from "./critical-path-types";
export function buildExecutionSurvivabilityForecast(milestones: CriticalMilestone[], fragility: ExecutionFragility, pressure: TemporalPressureCluster[]): ExecutionSurvivabilityForecast {
  const milestoneRisk = milestones.length ? milestones.reduce((s, m) => s + m.collapseLikelihood, 0) / milestones.length : 0.45;
  const survivabilityScore = Number(Math.max(0, 1 - ((milestoneRisk + fragility.fragilityScore + (pressure[0]?.pressureScore ?? 0.5)) / 3)).toFixed(2));
  return { survivabilityScore, expectedDegradationBand: survivabilityScore < 0.35 ? "immediate" : survivabilityScore < 0.55 ? "near_term" : "medium_term", collapseRisks: milestones.map((m) => ({ milestoneId: m.milestoneId, riskScore: m.collapseLikelihood, timeframe: m.collapseLikelihood > 0.8 ? "immediate" : "near_term", evidence: m.evidence })), evidence: [...fragility.evidence, ...(pressure[0]?.evidence ?? [])], confidence: 0.74, uncertainty: ["Forecast is bounded by available dependency evidence freshness."], causalityChain: fragility.causalityChain, propagationExplanation: "Propagation pressure and fragility jointly drive survivability decline.", survivabilityExplanation: "Survivability score is a bounded deterministic blend of milestone risk, fragility, and temporal pressure." };
}
