import type { InterventionEscalationPath } from "./autonomous-intervention-types";
export function buildEscalationPaths(): InterventionEscalationPath[] { return ["operational","governance","commercial","procurement","executive","technical"].map((pathType) => ({ pathType: pathType as InterventionEscalationPath["pathType"], triggerCondition: `${pathType}_pressure_persists`, targetRole: `${pathType}_owner`, evidenceRequired: ["continuity_record","critical_path_signal"], expectedImpact: `${pathType}_blockage_relief`, fallbackIfIgnored: `advance_to_next_${pathType}_chain`, recommendedTiming: pathType === "executive" ? "urgent" : "next_cycle" })); }
