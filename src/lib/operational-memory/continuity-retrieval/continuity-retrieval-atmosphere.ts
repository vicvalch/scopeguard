import type { ContinuityAtmosphereSummary, ContinuityInterventionSummary, ContinuityPressureSummary, ContinuityRetrievalCandidate } from "./continuity-retrieval-types";
export function summarizeAtmosphere(candidates: ContinuityRetrievalCandidate[], pressure: ContinuityPressureSummary, interventions: ContinuityInterventionSummary[]): ContinuityAtmosphereSummary { const pressureAccumulation = pressure.unresolvedCount + pressure.recurringEscalations * 2 + pressure.criticalPathBlockers * 2; const escalationDensity = candidates.length ? pressure.recurringEscalations / candidates.length : 0; const interventionExhaustion = interventions.filter((i)=>i.outcomeTrend==="degrading").length; const score = Math.min(100, Math.round(pressureAccumulation * 3 + escalationDensity * 20 + interventionExhaustion * 6)); const degradationIndicators = [ pressure.unresolvedCount > 0 ? { type:"unresolved_pressure", severity: pressure.unresolvedCount>6?"critical":"high", summary:"Unresolved pressure concentration is degrading continuity." } : null, interventionExhaustion>0 ? { type:"intervention_exhaustion", severity:"high", summary:"Repeated intervention failures indicate recovery fatigue." } : null, pressure.governanceGapCount>0 ? { type:"governance_instability", severity:"medium", summary:"Governance gaps are contributing to degradation." } : null ].filter(Boolean) as ContinuityAtmosphereSummary["degradationIndicators"];
  return { score, trajectory: score >= 60 ? "degrading" : score >= 35 ? "stable" : "improving", pressureAccumulation, escalationDensity, interventionExhaustion, degradationIndicators };
}
