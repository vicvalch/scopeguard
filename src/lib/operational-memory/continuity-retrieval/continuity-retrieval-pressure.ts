import type { ContinuityPressureSummary, ContinuityRetrievalCandidate } from "./continuity-retrieval-types";
export const summarizePressure=(c:ContinuityRetrievalCandidate[]):ContinuityPressureSummary=>({ unresolvedCount:c.filter((v)=>["unresolved","escalated"].includes(v.record.resolutionStatus)).length, recurringEscalations:c.filter((v)=>v.record.recordType==="escalation"&&v.recurrenceCount>1).length, criticalPathBlockers:c.filter((v)=>["blocker","dependency"].includes(v.record.recordType)&&v.priority==="highest").length, procurementPressureCount:c.filter((v)=>/procure|vendor|budget/i.test(v.record.summary)).length, governanceGapCount:c.filter((v)=>v.record.recordType==="governance_gap").length });
