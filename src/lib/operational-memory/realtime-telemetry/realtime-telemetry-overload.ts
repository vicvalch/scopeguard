import type { OperationalTelemetryDelta, PMOverloadTelemetry } from "./realtime-telemetry-types";
export const buildOverloadTelemetry=(d:OperationalTelemetryDelta[]):PMOverloadTelemetry=>{const n=d.filter(x=>x.metric.includes("pm")||x.metric.includes("coordination")).reduce((a,b)=>a+Math.max(0,b.delta),0);return {coordinationSaturation:n,interventionLoad:n*0.9,recoveryFatigue:n*0.7,executionExhaustion:n*0.8,evidence:["pm-coordination-signals"],confidence:0.75,uncertainty:["heuristic metric tags"],causalityRationale:["coordination pressure maps to overload"],significanceRationale:["burnout prevention"],governanceBoundaries:["workspace-scoped"]};};
