import type { EscalationSaturationTelemetry, OperationalTelemetryDelta } from "./realtime-telemetry-types";
export const buildEscalationTelemetry=(d:OperationalTelemetryDelta[]):EscalationSaturationTelemetry=>{const n=d.filter(x=>x.metric.includes("escalation")).reduce((a,b)=>a+Math.max(0,b.delta),0);return {congestion:n,fatigue:n*0.8,amplification:n*0.6,overloadRisk:Math.min(100,n*1.5),evidence:["escalation-deltas"],confidence:0.75,uncertainty:["keyword mapping"],causalityRationale:["escalation growth increases saturation"],significanceRationale:["destabilization detector"],governanceBoundaries:["no cross-tenant aggregation"]};};
