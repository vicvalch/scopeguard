import type { ExecutiveCommandContext } from "./executive-command-context";
import type { ExecutiveEscalationSummary, ExecutiveFragilitySignal, ExecutiveInstabilityZone, ExecutivePortfolioHealth } from "./executive-command-types";
export function buildExecutivePortfolioHealth(context: ExecutiveCommandContext, zones: ExecutiveInstabilityZone[], escalation: ExecutiveEscalationSummary): ExecutivePortfolioHealth { const instability = Math.min(0.95, zones.reduce((acc, z) => acc + (z.spreading ? 0.16 : z.systemic ? 0.1 : 0.05), 0)); return { instability, sharedBottlenecks: ["governance_decision_latency", "procurement_dependency_queue"], recurringGovernanceFailure: escalation.governanceCongestion, recurringProcurementPressure: zones.some((z) => z.zone.includes("procurement")) ? 0.72 : 0.4, pmCapacitySaturation: 0.69, drift: 0.62, evidence: context.evidence, confidence: 0.67, uncertainty: ["portfolio synthesis bounded by project-level quality"], causality: ["shared bottlenecks propagate instability"], governanceBoundaries: context.governanceBoundaries, survivabilityRationale: ["portfolio instability is the upper-bound risk driver"] }; }
export function buildExecutiveFragilitySignals(context: ExecutiveCommandContext, portfolio: ExecutivePortfolioHealth, zones: ExecutiveInstabilityZone[]): ExecutiveFragilitySignal[] { return [{ signal: "shared_bottleneck_fragility", fragilityScore: portfolio.instability, domains: ["delivery","governance"], evidence: context.evidence, confidence: 0.64, uncertainty: ["depends_on_dependency_graph_completeness"], causality: ["critical chain concentration reduces resilience"], governanceBoundaries: context.governanceBoundaries, survivabilityRationale: ["fragility reduces stabilizability"] },{ signal: "spreading_instability_fragility", fragilityScore: zones.filter((z) => z.spreading).length / Math.max(1,zones.length), domains: ["portfolio"], evidence: context.evidence, confidence: 0.64, uncertainty: ["zone tagging may under-detect weak spread"], causality: ["spread increases systemic failure likelihood"], governanceBoundaries: context.governanceBoundaries, survivabilityRationale: ["spread erodes recoverability"] }]; }
