import type { ExecutiveCommandContext } from "./executive-command-context";
import type { ExecutiveOperationalFocus, ExecutiveStabilizationPriority } from "./executive-command-types";
export function buildExecutiveOperationalFocus(context: ExecutiveCommandContext, priorities: ExecutiveStabilizationPriority[]): ExecutiveOperationalFocus { const focusNow = priorities.slice(0, 3).map((p) => p.action); return { focusNow, rationale: ["sorted by deterministic stabilization priority score"], nextBestActions: ["protect_critical_milestone_chain","reduce_escalation_loop_reentry"], evidence: context.evidence, confidence: 0.69, uncertainty: ["focus quality depends on upstream signal fidelity"], causality: ["higher priority score implies higher stabilization leverage"], governanceBoundaries: context.governanceBoundaries, survivabilityRationale: ["focus allocation maximizes stabilization probability"] }; }
