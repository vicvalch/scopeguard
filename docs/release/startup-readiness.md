# Startup Readiness

Startup assertions are enforced by adapter bootstrap and runtime composition checks.

## Commands
- `npm run test:launch-smoke`
- `npm run diag:runtime`

If assertions fail, commands exit non-zero with categorized failure output.
